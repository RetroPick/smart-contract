{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "nest build",
    "start": "nest start",
    "start:dev": "nest start --watch",
    "test": "node -r ts-node/register --test src/app.service.spec.ts",
    "prisma:generate": "DATABASE_URL=${DATABASE_URL:-postgresql://retropick:retropick@localhost:5432/retropick} prisma generate",
    "prisma:migrate": "DATABASE_URL=${DATABASE_URL:-postgresql://retropick:retropick@localhost:5432/retropick} prisma migrate dev"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "@prisma/client": "^6.19.2",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.0",
    "@nestjs/schematics": "^11.0.0",
    "@nestjs/testing": "^11.0.0",
    "@types/node": "^22.13.10",
    "prisma": "^6.19.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  }
}
